#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main (void)
{
    // where the * are with type or name is the same like below
    // always initialize all ptrs
    // uninit ptrs contains garbage data and can be pointing anywhere 

    int* intPtr {};
    double *doublePtr {nullptr};
    char* charPtr {nullptr};
    string* stringPtr {nullptr};


    return 0;
}